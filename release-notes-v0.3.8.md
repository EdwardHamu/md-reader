## 新增
- 导出 PNG 长图：导出菜单新增「导出 PNG 长图」，将整篇渲染结果保存为一张所见即所得的长图（含公式、图表、代码高亮、表格），默认 2 倍缩放，超长文档自动适配（#31）。

## 修复
- macOS：双击 `.md` 文件现在会自动用 MD Reader 打开该文件，不再需要手动拖入。
- 修复偶发的「双击 exe 没有反应、exe 文件无法删除（提示正在使用）」的问题：应用启动和再次唤起时都会兜底确保主窗口可见且位于屏幕内（#32）。
